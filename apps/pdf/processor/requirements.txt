fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
python-multipart>=0.0.9,<1
# OCRmyPDF 16+ requires Python 3.10+. Keep the lower bound compatible with
# the Python 3.9 developer environment; the production image uses 3.12 and
# resolves the current 16.x release.
ocrmypdf>=15.4.4,<18
